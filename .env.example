# ApplyPilot currently runs without required environment variables.
# Leave this file blank to use the built-in deterministic grounded-answer generator.

# Optional: short-answer provider selector.
# Supported local value today: deterministic
APPLYPILOT_SHORT_ANSWER_PROVIDER=
